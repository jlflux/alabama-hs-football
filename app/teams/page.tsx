import Link from 'next/link';import {teams} from '@/lib/demo-data';
export default async function Teams({searchParams}:{searchParams:Promise<{q?:string}>}){const {q=''}=await searchParams;const filtered=teams.filter(t=>`${t.name} ${t.mascot} ${t.city}`.toLowerCase().includes(q.toLowerCase()));return <><div className="pagehead"><div className="wrap"><h1>Teams</h1><div className="muted">Permanent profiles for every Alabama high school football program.</div></div></div><main className="wrap section"><form className="search" style={{border:'1px solid #e3e6eb',marginBottom:18}}><input name="q" defaultValue={q} placeholder="Search teams…"/><button>Search</button></form><div className="grid">{filtered.map(t=><Link className="card team-row" href={`/teams/${t.slug}`} key={t.id}><div className="logo">{t.name.slice(0,2).toUpperCase()}</div><div><h3>{t.name} {t.mascot}</h3><div className="muted">{t.classification} · Region {t.region} · {t.city}</div></div></Link>)}</div></main></>}
