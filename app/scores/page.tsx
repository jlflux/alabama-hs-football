import {games,teamById} from '@/lib/demo-data';export default function Scores(){return <><div className="pagehead"><div className="wrap"><h1>Scores</h1><div className="muted">Statewide score center · demo data</div></div></div><main className="wrap section"><div className="scores">{games.map(g=>{const h=teamById(g.homeTeamId)!;const a=teamById(g.awayTeamId)!;return <div className="card" key={g.id}><div className="muted">Week {g.week} · {g.date}</div><div className="game"><b>{a.name}</b><span className="score">{g.status==='final'?g.awayScore:'—'}</span></div><div className="game"><b>{h.name}</b><span className="score">{g.status==='final'?g.homeScore:'—'}</span></div></div>})}</div></main></>}