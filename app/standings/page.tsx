import Link from 'next/link';import {standings,teamById} from '@/lib/demo-data';
export default function Standings(){return <><div className="pagehead"><div className="wrap"><h1>Region Standings</h1><div className="muted">2026 · Demo classification/region data</div></div></div><main className="wrap section"><h2 style={{marginBottom:14}}>5A Region 4</h2><table className="table"><thead><tr><th>Team</th><th>Region</th><th>Overall</th></tr></thead><tbody>{standings.map(s=>{const t=teamById(s.teamId)!;return <tr key={s.teamId}><td><Link href={`/teams/${t.slug}`}><b>{t.name}</b></Link></td><td>{s.regionWins}-{s.regionLosses}</td><td>{s.overallWins}-{s.overallLosses}</td></tr>})}</tbody></table></main></>}
